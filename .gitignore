# Environment Variables
.env # Ignore .env if it's at the root
capstone/.env # Ignore .env if it's inside the first capstone dir
capstone/capstone/.env # Ignore .env if inside the inner capstone dir

# Python Virtual Environment (relative to root)
capstone/HH/

# Python cache files (recursive)
**/__pycache__/
*.py[cod]

# Databases (if local only, relative to root)
capstone/db.sqlite3*
capstone/test_db.sqlite3

# Google Cloud & GitGuardian specific ignores (assuming at root)
.gcloudignore
.ggignore
.cache_ggshield
# Also ignore the one found inside capstone, if it exists
capstone/.gcloudignore

# Log files (relative to root -> inner capstone dir)
capstone/capstone/log_files/

# Scraper output data (relative to root -> commands dir)
capstone/capstone/management/commands/scraped_data/

# Other JSON files in commands dir (relative to root -> commands dir)
capstone/capstone/management/commands/*.json

# Database Backups (relative to root -> inner capstone dir)
capstone/capstone/database_backups/

# Collected Static Files (relative to root -> inner capstone dir)
capstone/capstone/staticfiles/

# Other specific files (relative to root)
capstone/maze.py # Assuming maze.py is inside the first capstone dir

# VS Code specific
.vscode/

# OS specific
.DS_Store
Thumbs.db